# CI — ruff lint + pytest.
#
# The whole suite (unit + integration) is hermetic: integration tests run
# against a localhost MockGub server (tests/integration/conftest.py), so no
# credentials, GCP access, or live GUB backend is needed.

name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      # 3.12 matches the Dockerfile; pyproject requires >=3.11.
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package + dev dependencies
        run: pip install -e ".[dev]"

      - name: Lint (ruff)
        run: ruff check gub_agent tests

      - name: Tests (pytest)
        run: pytest
