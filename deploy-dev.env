# Deploy-time env baked into the DEV reasoning engine via `adk deploy --env_file`.
# Everything else comes from config.py defaults; the model location is pinned in
# code (config.py) because it's a property of the model, not the environment.
# Keep this minimal and secret-free — it ships into the engine.
EMIT_THINKING=1
